import { ColorChangePipe } from './color-change.pipe';

describe('ColorChangePipe', () => {
  it('create an instance', () => {
    const pipe = new ColorChangePipe();
    expect(pipe).toBeTruthy();
  });
});
